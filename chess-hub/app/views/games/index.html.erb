<%= link_to "New Game", new_game_path %>

<h2>Existing Games</h2>
<ul>
  <% @games.order(created_at: :desc).each do |game| %>
    <li>
      <%= link_to "Game ##{game.id} (#{game.moves.count} moves)", game_path(game) %>
    </li>
  <% end %>
</ul>
